Projection Ln+1 → Ln as Invariant Folding

Статус: архитектурный принцип
Контекст: DETM / иерархические динамические системы
Назначение: формализация коарсинга как проекции уровней

1. Мотивация

При переходе между уровнями в иерархической системе возникает вопрос:

что именно сохраняется;

что теряется;

и почему такой переход допустим без потери “реальности” объекта.

Данный принцип формализует переход
от уровня 
𝐿
𝑛
+
1
L
n+1
	​

 к уровню 
𝐿
𝑛
L
n
	​


как проекцию по эквивалентности, а не как уничтожение информации.

2. Основная идея

Проекция 
𝐿
𝑛
+
1
→
𝐿
𝑛
L
n+1
	​

→L
n
	​

 — это
свёртка множества внутренних состояний
в один инвариант уровня 
𝐿
𝑛
L
n
	​

,
где теряется не информация, а различимость.

Иначе говоря:

информация остаётся в системе,

но становится неразличимой на данном масштабе.

3. Уровни и внутренняя динамика

Уровень 
𝐿
𝑛
+
1
L
n+1
	​

 содержит:

внутреннее время,

фазу,

микродинамику,

множество допустимых траекторий.

Уровень 
𝐿
𝑛
L
n
	​

 различает:

только устойчивые классы состояний,

только инварианты,

только эффекты, пережившие коарсинг.

Следствие:

𝐿
𝑛
L
n
	​

 не “видит” время 
𝐿
𝑛
+
1
L
n+1
	​

,
он видит результат его проживания.

4. Проекция как факторизация

Формально:

пусть 
𝑆
𝑛
+
1
S
n+1
	​

 — пространство состояний уровня 
𝐿
𝑛
+
1
L
n+1
	​

;

вводится отношение эквивалентности

𝑠
1
∼
𝑠
2
s
1
	​

∼s
2
	​

, если они неразличимы на уровне 
𝐿
𝑛
L
n
	​

;

тогда состояние уровня 
𝐿
𝑛
L
n
	​

 — это фактор-множество:

𝑆
𝑛
=
𝑆
𝑛
+
1
/
∼
S
n
	​

=S
n+1
	​

/∼

Проекция:

𝜋
:
𝑆
𝑛
+
1
→
𝑆
𝑛
π:S
n+1
	​

→S
n
	​

5. Аналогия с комплексными числами (минимальный пример)

Комплексное число:

𝑧
=
𝑟
𝑒
𝑖
𝜃
z=re
iθ

𝑟
r — инвариант (видим на 
𝐿
𝑛
L
n
	​

),

𝜃
θ — внутренняя фаза (жизнь 
𝐿
𝑛
+
1
L
n+1
	​

).

Проекция:

𝐿
𝑛
L
n
	​

 различает 
𝑟
r,

𝐿
𝑛
+
1
L
n+1
	​

 живёт в 
𝜃
θ.

Комплексное представление — минимальный пример
свёртки уровня с сохранением инварианта.

6. Возведение в степень как продвижение по уровням

Итерация:

𝑧
𝑘
=
𝑟
𝑘
𝑒
𝑖
𝑘
𝜃
z
k
=r
k
e
ikθ

Интерпретация:

𝑘
k — число применений минимального оператора;

𝑘
𝜃
kθ — накопленное внутреннее время;

𝑟
𝑘
r
k
 — усиленный или ослабленный инвариант.

Проекция обратно в 
𝐿
𝑛
L
n
	​

:

забывает фазу,

сохраняет инвариант.

7. Связь с коарсингом

Коарсинг — это не “сжатие” и не “приближение”, а:

операция факторизации по неразличимости.

Коарсинг допустим только если:

эквивалентные состояния устойчивы;

проекция воспроизводима;

инвариант переживает прямую симуляцию нижнего уровня.

8. Потери при проекции

При переходе 
𝐿
𝑛
+
1
→
𝐿
𝑛
L
n+1
	​

→L
n
	​

 теряется:

информация о фазе;

информация о микротраектории;

локальное внутреннее время.

Не теряется:

устойчивость;

инвариант;

причинная валидность.

Теряется не “что”,
а “когда и как именно”.

9. Связь с временем как степенью

Принцип согласуется с каноном:

время = длина цепочки операторов;

уровень = степень устойчивости;

проекция = забывание внутреннего времени.

Ln — это не “раньше”,
а “менее глубокая степень”.

10. Отличие от классического представления времени

Классика:

время — внешняя ось;

состояние — точка.

Здесь:

время — внутренняя фаза;

состояние — класс эквивалентности.

Следствие:

Нельзя восстановить 
𝐿
𝑛
+
1
L
n+1
	​

 из 
𝐿
𝑛
L
n
	​


без проживания дополнительного времени.

11. Канонические формулы

Краткие формулировки принципа:

Проекция уровня — это забывание фазы,
а не уничтожение состояния.

Ln видит инвариант,
Ln+1 живёт в его фазе.

Коарсинг — это факторизация по времени.

12. Статус

Принцип считается:

каноническим для DETM;

обязательным для описания переходов между уровнями;

совместимым с принципами:

Time as Operator Chain,

Level Reality via Live Coarsening,

Stability by Ignoring.
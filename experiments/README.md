# Experiments

Protocol entrypoints:

- `python -m experiments.marker_protocol`
- `python -m experiments.scaling_protocol`

Golden baseline:

- config: `experiments/00_baseline/config.json`
- how to run: `experiments/00_baseline/README.md`
